1.项目根目录创建 .devcontainer
---

Dockerfile文件：
---

    # .devcontainer/Dockerfile
    # 从12.1.0升级到 CUDA 12.4.1，彻底镇压 half 精度编译歧义问题
    FROM nvidia/cuda:12.4.1-devel-ubuntu22.04

    ENV DEBIAN_FRONTEND=noninteractive

    RUN apt-get update && apt-get install -y \
        build-essential cmake git curl wget sudo \
        python3 python3-pip python3-dev \
        && rm -rf /var/lib/apt/lists/*

    ARG USERNAME=hehe
    ARG USER_UID=1000
    ARG USER_GID=$USER_UID

    RUN groupadd --gid $USER_GID $USERNAME \
        && useradd --uid $USER_UID --gid $USER_GID -m -s /bin/bash $USERNAME \
        && echo "$USERNAME ALL=(root) NOPASSWD:ALL" > /etc/sudoers.d/$USERNAME \
        && chmod 0440 /etc/sudoers.d/$USERNAME

    USER $USERNAME
    WORKDIR /home/$USERNAME

    RUN curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y
    ENV PATH="/home/${USERNAME}/.cargo/bin:${PATH}"
    ENV PYO3_PYTHON=/usr/bin/python3

    ARG CUDA_ARCH=89

    # --- 终极物理防弹衣：双重变量齐下，彻底镇压链接器 ---
    # 1. 编译期直接依赖寻址
    ENV LIBRARY_PATH=/usr/local/cuda/lib64/stubs:$LIBRARY_PATH
    # 2. 编译期传递依赖验证 & 运行期寻址
    ENV LD_LIBRARY_PATH=/usr/local/cuda/lib64/stubs:$LD_LIBRARY_PATH
    # 3. 欺骗系统，伪造驱动
    RUN sudo ln -s /usr/local/cuda/lib64/stubs/libcuda.so /usr/local/cuda/lib64/stubs/libcuda.so.1 || true

    # 执行编译（斩断网络代理，多核全开，保留全部官方测试玩具！）
    RUN sudo mkdir -p /opt/llama.cpp \
        && sudo chown -R $USERNAME:$USERNAME /opt \
        && export http_proxy="" && export https_proxy="" && export HTTP_PROXY="" && export HTTPS_PROXY="" \
        && git clone https://github.com/ggml-org/llama.cpp.git /opt/llama.cpp \
        && cd /opt/llama.cpp \
        && cmake -B build -DGGML_CUDA=ON -DCMAKE_CUDA_ARCHITECTURES=${CUDA_ARCH} \
        && cmake --build build --config Release -j $(nproc)
        
---

devcontainer.json文件：

除RTX4000的其他显卡需修改"CUDA_ARCH": "89"中参数

---

    // .devcontainer/devcontainer.json
    {
    "name": "Edge AI Causal FMEA Dev",
    "build": {
        "dockerfile": "Dockerfile",
        "context": "..",
        "args": {
            "CUDA_ARCH": "89"   // 在除RTX4000系显卡上需修改，例如3000系为86
        }
    },
    
    // 强制指定我们刚刚创建的用户，告别 root 裸奔
    "remoteUser": "hehe",

    // 极限硬件压榨：将宿主机的 GPU 物理直通进容器 (针对你的 4060 必须开启)
    "runArgs": [
        "--gpus", "all",
        "--shm-size", "8g" // 增加共享内存，防止多线程张量处理时崩溃
    ],

    // 挂载工作区
    "workspaceMount": "source=${localWorkspaceFolder},target=/workspaces/causal_fmea_system,type=bind,consistency=cached",
    "workspaceFolder": "/workspaces/causal_fmea_system",

    // 预装我们开发所需的底层与顶层推演神器
    "customizations": {
        "vscode": {
        "extensions": [
            "ms-python.python",        // Python 顶层 Agent 开发
            "rust-lang.rust-analyzer", // Rust 底层 FMEA 引擎开发必备
            "ms-vscode.cpptools",      // llama.cpp 源码阅读与微调
            "charliermarsh.ruff"       // 强制 PEP8 静态审查工具 [cite: 205]
        ],
        "settings": {
            "python.defaultInterpreterPath": "/usr/bin/python3",
            "rust-analyzer.cargo.features": "all"
        }
        }
    },

    // 容器启动后，自动升级 pip 并安装 maturins 编译脚手架 [cite: 99]
    "postCreateCommand": "pip3 install --upgrade pip && pip3 install maturin pytest"
    }


---

结束后终端里敲下 rustc --version 和 nvidia-smi 验证
-

---

验证阶段
-

1. 验证 Rust 跨语言桥梁脚手架 (maturin)：

        Bash
        maturin --version
预期体感：秒出 maturin 1.x.x。这证明 Python 和 Rust 之间的跨语言打包工具已经就位，这是我们后续干翻 Python GIL 锁的资本。

----------------------------------------------------------
2. 验证 C++ 核心库是否真的链接到了真显卡：

        Bash
        ldd /opt/llama.cpp/build/bin/llama-cli | grep cuda
预期体感：必须看到 libcuda.so.1 => /usr/lib/wsl/lib/libcuda.so.1 或者类似的真实驱动路径！这证明我们在 docker run 阶段，已经成功扒下了之前编译时的“假驱动”伪装，真正接管了 4060 的物理硬件！

但是问题来了，libcuda.so.1 => /usr/local/cuda/lib64/stubs/libcuda.so.1
这证明虽然你的 4060 硬件已经挂载进来了，但你的 llama-cli 玩具，此时此刻依然瞎了眼，认贼作父，连着那个不能做任何算力推理的“假驱动（stubs）”！

不用手动执行，已经写入Dockerfile自动执行了
-

接下来执行：
下一步动作（极其暴力的物理切除）：
在终端里直接一枪干掉这个假驱动的软链接！执行：

        Bash
        sudo rm /usr/local/cuda/lib64/stubs/libcuda.so.1

        删掉之后，再次执行：

        Bash
        ldd /opt/llama.cpp/build/bin/llama-cli | grep cuda


        会在终端中找到：libcuda.so.1 => /usr/lib/x86_64-linux-gnu/libcuda.so.1


---

安装QWen2.5-7b-q4km量化：

    wget https://hf-mirror.com/bartowski/Qwen2.5-7B-Instruct-GGUF/resolve/main/Qwen2.5-7B-Instruct-Q4_K_M.gguf -O qwen2.5.gguf


运行model：

    /opt/llama.cpp/build/bin/llama-cli -m qwen2.5.gguf -ngl 999 -c 4096 -p "系统提示：你现在是 CausalForge 通用因果推演系统。请分析以下事件的因果链条：【传感器 A 数据异常导致冷却系统停机，最终引发反应堆过热。】\n请输出推演结果：" -n 200

显存占用：6058MiB /   8188MiB 

---

命令用于创建一个全新的、使用 PyO3 绑定的 Rust-Python 混合项目

    maturin new --bindings pyo3 causal_fmea_core  

    causal_fmea_core/
    ├── Cargo.toml      # Rust 项目配置，已包含 pyo3 依赖
    ├── pyproject.toml  # Python 打包配置，指定 maturin 作为构建后端
    └── src/
        └── lib.rs      # Rust 源代码模板，包含 PyO3 模块初始化代码
---
---

 1.战役一——字符串驻留池（String Interning Pool）设计
 -


我们在选择跨语言数据传输策略时，如果大模型在 Python 侧疯狂吐出因果图节点（如 "冷却系统异常", "反应堆熔毁"），如果把这些沉重的字符串（String）全部深拷贝扔给 Rust， 4060 剩下的 2GB 显存和内存会被海量的内存碎片瞬间撑爆（OOM）。

所以坚决抵制字符串！建立全局字典（驻留池）。 Python 传进字符串，Rust 没收它，并扔回一个极度轻量的整数编号（如 0, 1）。以后的 FMEA 拦截和图论计算，全在 CPU 寄存器里用光速的整数对比完成！

1.Cargo.toml 里引入了 indexmap::IndexSet
-
    Bash
    # 压入 PyO3 (并开启 Python 3.10 ABI 扩展模块特性)
    cargo add pyo3@0.20.2 --features extension-module,abi3-py310

    # 压入驻留池神器 indexmap
    cargo add indexmap@2.1.0

2.在lib.rs中修改代码
-

    use pyo3::prelude::*;
    use indexmap::IndexSet;
    use std::sync::{Arc, Mutex};   // Arc（原子引用计数指针）和 Mutex（互斥锁）

    #[pyclass]
    pub struct CausalParadigmEngine {   // 声明 Rust 结构体可以暴露给 Python 使用
        interner: Arc<Mutex<IndexSet<String>>>,  // 类型为 Arc<Mutex<IndexSet<String>>>，即线程安全、可共享的字符串集合
    }

    #[pymethods]  // 声明这是实现的方法
    impl CausalParadigmEngine {     // 实现 CausalParadigmEngine 的方法，并暴露给 Python
        #[new]  // 指定为 Python 构造函数
        pub fn new() -> Self {  // 返回一个新的 CausalParadigmEngine 实例 就像Py中的 __init__ 方法一样
            println!("⚡ [CausalForge] 正在初始化 EdgeThemis 引擎...");
            println!("⚡ [CausalForge] 正在开辟底层字符串驻留池...");
            CausalParadigmEngine {
                interner: Arc::new(Mutex::new(IndexSet::new())),  // 初始化为一个空的 IndexSet，并用 Arc 和 Mutex 包裹，保证线程安全和可共享
            }
        }

        /// 任务 1：注册节点
        pub fn register_node(&self, node_name: String) -> PyResult<usize> {
            // 你的逻辑 1：极其粗暴地拿锁，并声明为可变 (mut)
            let mut pool = self.interner.lock().unwrap();
            
            // 你的逻辑 2：插入数据，并用 .0 提取第一个元素 (ID)  insert_full 返回 (index, bool),取下标
            let id = pool.insert_full(node_name).0;
            
            Ok(id)
        }

        /// 任务 2：反向查询
        pub fn get_node_name(&self, node_id: usize) -> PyResult<Option<String>> {
            // 拿锁（只读，所以不需要 mut）
            let pool = self.interner.lock().unwrap();
            
            // 获取引用，并克隆一份 String 扔回给 Python 把内部数据“拷贝”出来，安全返回给 Python
            Ok(pool.get_index(node_id).cloned())
        }
    }

    #[pymodule]  // 声明为 Python 模块初始化函数
    fn causal_fmea_core(_py: Python, m: &PyModule) -> PyResult<()> {
        m.add_class::<CausalParadigmEngine>()?;
        Ok(())
    }

位于 causal_fmea_core 目录内把Rust打包成Python能用的库
-
    Bash
    maturin develop --release

遇到问题，显示报错：

    hehe@0897e4ba1324:/workspaces/causal_fmea_system/causal_fmea_core$ maturin develop --release
    💥 maturin failed
    Caused by: Couldn't find a virtualenv or conda environment, but you need one to use this command. For maturin to find your virtualenv you need to either set VIRTUAL_ENV (through activate), set CONDA_PREFIX (through conda activate) or have a virtualenv called .venv in the current or any parent folder. See https://virtualenv.pypa.io/en/latest/index.html on how to use virtualenv or use `maturin build` and `pip install <path/to/wheel>` instead.

maturin develop 的物理动作是“编译并立刻安装”，但它环顾四周，发现你并没有给它准备一个隔离的“沙盒（Virtual Environment）”，所以它直接拔枪逼停了你的操作

解决方法：建立虚拟环境运行

    第一步：把缺失的 venv 模块强行装回物理机
    sudo apt-get update && sudo apt-get install python3.10-venv -y

    建立物理隔离沙盒 (Venv)
    python3 -m venv .venv

    激活沙盒权限
    source .venv/bin/activate

    再次执行
    maturin develop --release

以上的问题到最后在Dockerfile中自动执行，别人只需要build就行
-

---

2.战役二：极速 FMEA 量化算子
-
用 Rust 徒手搓出这个“计算总风险值 (RPN)”的公式

    RPN = S * O * D

    S (Severity, 严重度)：后果有多惨？

    例子：报废一个灯泡，S=2；反应堆爆炸炸死人，S=10。

    O (Occurrence, 频发度)：这事儿有多容易发生？

    例子：百年一遇的陨石砸中机房，O=1；每天都漏水的破管子，O=9。

    D (Detection, 探测度)：⚠️ 注意，这个值是反直觉的！ 它代表“在灾难发生前，系统有多难发现它”。

    例子：有高精度警报器秒级拦截，D=1；隐蔽的底层内存泄漏（神不知鬼不觉），D=9。

为了防止极端情况发生

    采用“严重度绝对偏置算法”：RPN = (100 * S) + (10 * O) + D


在lib.rs中添加类声明及其实现方法：

    #[pyclass]
    #[derive(Clone, Copy, Debug)]
    pub struct FmeaScore {
        #[pyo3(get, set)] pub s: u32, // 让 s、o、d 这三个字段在 Python 侧可以直接读写（即 obj.s、obj.s = 1）。
        #[pyo3(get, set)] pub o: u32,
        #[pyo3(get, set)] pub d: u32,
    }

    #[pymethods]
    impl FmeaScore {
        #[new]
        pub fn new(s: u32, o: u32, d: u32) -> Self {
            FmeaScore { s, o, d }
        }

        /// 核心任务：用 Rust 确立裁判法则
        pub fn calculate_rpn(&self) -> u32 {
            // 隐式返回法则：这行不加分号，直接将其作为 u32 结果扔给 Python
            (100 * self.s) + (10 * self.o) + self.d
        }
    }

并在最后module初始化函数里注册这个类：

    #[pymodule]  // 声明为 Python 模块初始化函数
    fn causal_fmea_core(_py: Python, m: &PyModule) -> PyResult<()> {
        m.add_class::<FmeaScore>()?;
        m.add_class::<CausalParadigmEngine>()?;
        Ok(())
    }

---
---
3.战役三：物理死神拦截 (The Reaper)
-



